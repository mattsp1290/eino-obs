# Release And Consumer Pinning

Use this file for first-release tagging and the response format that
`eino-agent` can pin.

## Pending Content

- First integration tag or commit decision.
- Semver-style tag plan, such as `v0.1.0`, when the repository is ready.
- Where README or release notes record the exact tag or commit.
- Response format for telling `eino-agent` which version to pin.
